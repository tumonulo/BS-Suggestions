<div>
  <header class="flex justify-between items-center bg-blue-600 text-white p-4">
    <h2 class="font-bold text-lg">BS Suggestions</h2>

    <div class="flex items-center gap-8">
      <nav class="flex gap-4">
        <a href="/" class="hover:underline">Inicio</a>
        <a href="/suggestions" class="hover:underline">Sugerencias</a>
      </nav>
      <button type="button" class="px-4 py-2 bg-blue-600 text-white rounded">
        Login
      </button>
    </div>
  </header>

  <div class="fixed inset-0 bg-black bg-opacity-50 hidden items-center justify-center z-50">
    <div class="bg-white rounded-lg p-6 w-96">
      <h2 class="text-xl font-bold mb-4">Iniciar sesión</h2>

      <p class="text-red-600 mb-2" aria-live="polite"></p>

      <form method="POST" class="flex flex-col gap-2">
        <input name="email" type="email" placeholder="Email" class="w-full p-2 border rounded" required />
        <input name="password" type="password" placeholder="Password" class="w-full p-2 border rounded" required />

        <div class="flex justify-end gap-2 mt-2">
          <button type="button" class="px-4 py-2 border rounded">Cancelar</button>
          <button type="submit" class="px-4 py-2 bg-blue-600 text-white rounded">Login</button>
        </div>
      </form>

      <hr class="my-4" />

      <form method="POST">
        <button type="submit" class="w-full px-4 py-2 bg-purple-600 text-white rounded">
          Iniciar con Discord
        </button>
      </form>
    </div>
  </div>
</div>