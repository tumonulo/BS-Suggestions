---
const suggestions = [
  { title: "New weekly map", votes: 120, author: "Juanito" },
  { title: "Balance Bibi", votes: 80, author: "MariaBS" },
  { title: "More skins", votes: 50, author: "Carlos" },
]
const sortedSuggestions = suggestions.sort((a, b) => b.votes - a.votes)
---

<section class="bg-gray-50 text-gray-900 py-16">
  <div class="max-w-4xl mx-auto px-6">
    <div class="flex justify-between items-center mb-6">
      <h3 class="text-3xl font-bold">Sugerencias</h3>
      <button class="bg-gray-200 text-gray-900 px-4 py-2 rounded hover:bg-gray-300">
        Esto para alternar entre nuevas y mas votadas
      </button>
    </div>

    <div class="grid gap-4">
      {sortedSuggestions.map(s => (
        <div class="bg-white p-4 rounded-xl shadow flex justify-between items-start">
          <div>
            <h4 class="font-bold text-lg mb-1">{s.title}</h4>
            <p class="text-gray-700 mb-1">by {s.author}</p>
          </div>
          <div class="text-yellow-500 font-bold">⭐ {s.votes}</div>
        </div>
      ))}
    </div>
  </div>
</section>